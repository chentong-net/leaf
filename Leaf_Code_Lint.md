## Leaf Code Lint

### 代码规范

- 代码采用C++语言开发

- 代码的实现应达到生产级、工业级，在生产环境中保持运行稳定，应当具备完备性、健壮性，并在尽可能的条件下达到完美

- 代码量不设上限

- 如果涉及到未来可能要更改（实现）的代码（也就是争议代码），那么，若争议代码已存在，就在该行代码上方使用TODO注释（如：// TODO: 注释内容）进行标注，若还未存在，则在需要编写代码的位置使用TODO注释进行标注，但请注意，争议代码非必要禁止出现，因为对于未来维护会有很大问题

- 注释内容非专有名词（变量名、类名、关键字等）部分应该采用中文，注释内容中英文之间不需要空格，示例如下

    ```cpp
    // ❌ 创建一个 Buffer 缓冲区
    // ✅ 创建一个Buffer缓冲区
    ```

- 注释内容需避免主观词汇（如：我做了xxx、这里是xxx功能等），可以没有主语，但一定要要客观陈述（如：计算xxx函数、xxx结构等）

- 注释应避免出现分点（对于步骤性的代码，不要故意在注释前标注1、2、3这样的点，避免后期维护时更改代码导致逻辑不通顺）

- 函数内的注释应一律使用单行`// + 空格`进行注释（如：// 注释内容）

- 函数/类的说明注释一律采用以下格式

    ```cpp
    /**
    * 注释
    */
    ```

- 函数间的注释应一律使用单行`// + 空格`进行注释，以下为示例
  
    ```cpp
    int funcA() {}

    // 函数间注释

    int funcB() {}
    ```

- 不可出现类似以下形式的注释

    ```cpp
    //====================
    // 注释
    //====================
    ```

- Windows端使用GCC进行编译